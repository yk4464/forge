# forge ROADMAP — 功能规划与实现顺序

> 状态：`[x]` 已有基础实现 · `[~]` 部分完成或存在已知缺口 · `[ ]` 待实现。状态不代表已经通过生产环境验收。
> 本次更新只调整规划与文档，不表示功能已经实现或缺陷已经修复。
> `S0`～`S7` 是本路线图的实施阶段，不是版本号，也不沿用原型阶段的 Milestone 1 编号。按依赖推进，不承诺日历工期。

## 实现顺序与阶段验收

建议主线：**S0 修复基线 → S1 可控运行与数据可靠性 → S2 安全编程闭环 → S3 日常可用 → S4 扩展生态 → S5 多 agent 协作 → S6 多端交付**。S7 按需求选做。

| 阶段 | 阶段内实现顺序 | 验收标准 |
|---|---|---|
| **S0 修复基线** | 补针对性回归用例 → shell 实时输出与内存上限 → 错误与结束状态收口 → 压缩时规则和工具消息完整性 | 长命令结束前能看到输出；持续大输出时捕获内存有界；错误后 TUI 可再次提交；压缩后系统规则仍在、无悬空工具结果 |
| **S1 可控运行与数据可靠性** | 运行状态、标识和取消协议 → 数据版本与迁移基础 → 原始记录和模型上下文分离 → 工具执行日志与恢复 → 任务预算／循环检测 → 最小命令与事件接口 | 可取消请求、工具与子进程树；强退后可恢复记录；执行结果不明的有副作用操作不自动重放；超预算可停止且说明原因；存储失败明确告知用户 |
| **S2 安全编程闭环** | 权限策略与工作区边界 → Read／Grep／Glob → Edit／Write 与 diff → 最小 AGENTS.md／系统提示词 → 提问／Todo／计划模式 → 安全检查点与回退 | 完成“读代码—定位—审批—修改—构建／测试—展示 diff”；拒绝操作不会落盘；计划模式不能绕过权限写入；回退保留用户原有及后来手动改动 |
| **S3 日常可用** | 请求分类重试与能力校验 → 会话管理 → 文件引用与上下文可视化 → TUI 编辑与渲染 → 缓存／用量统计 → 脚本接口／doctor／基础发行包 | Windows 下能安装并完成固定编程任务；多轮、取消后继续、跨项目恢复、长会话滚动和大段中文粘贴可用；用量累计正确；JSONL 可解析、退出码明确 |
| **S4 扩展生态** | 配置与兼容规范 → Skills 按需加载 → MCP 工具接入 → Hooks → 后台任务管理／独立只读工具并发 → WebSearch／WebFetch | 代表性 skill、MCP server 和 hook 有兼容测试；扩展遵守同一权限和预算；超时可取消；后台任务可查询、停止并清理 |
| **S5 多 agent 协作** | worktree 隔离 → Task／子代理 → 结果审阅与集成 → 同机跨终端消息 → 协议文档 | 子任务有并发／层级／总预算上限；取消可传播；任务改动不覆盖彼此；冲突可见且由明确流程处理；跨终端消息不串会话 |
| **S6 多端交付** | 完善传输协议与本地服务 → Web UI（Axum + React）→ Tauri 2 桌面端 | TUI／Web／桌面复用运行、审批、取消与会话逻辑；重连无漏事件或重复提交；本地服务有访问认证与来源限制；窗口关闭不遗留失管任务 |
| **S7 按需增强** | 依据实际使用选择 LSP／符号索引、PTY、OAuth／订阅额度、额外平台、自动更新等 | 每项独立验收；不以是否对齐其他产品的功能数量作为完成标准 |

依赖说明：S0～S4 按主线推进；S5 与 S6 都建立在 S1 的运行协议及 S4 的扩展能力之上，**S6 不依赖 S5**，可按需求调换。多端接口在 S1 预留，界面实现留到 S6。测试、诊断和性能检查随各阶段交付；发行包从 S3 开始提供。

执行约定：一次完成一个可独立验收的小项；新增能力同时补对应故障场景和验收证据。各节标注阶段，跨阶段条目分步完成，所有子项通过后才勾选。仅记录实际执行过的验证，不把静态审查写成运行测试通过。

## 0. 现状基线（v0.0.1）

- [x] 流式 agent 循环 + 统一 `AgentEvent` 事件总线。
- [x] 三个 SSE 协议面：`/v1/chat/completions`、`/v1/responses`、`/v1/messages`；具体模型兼容性需逐项验证。
- [~] shell 工具：已有 Git Bash 执行、超时与结果截断；实时转发和捕获内存限制存在静态审查发现，见下方 S0。
- [~] 上下文压缩：已有 pre-turn／工具输出后检查及 `/compact`；规则保留、消息配对、原始历史保真仍需补齐。
- [~] SQLite 会话持久化（WAL）：已有 `/resume`、跨进程加载和 `forge check`；尚不等于运行中任务恢复或完整历史归档。
- [x] Ratatui TUI 基础：流式正文／推理内容／工具卡片，`/new`、`/resume`、`/compact`、`/exit`、`/help`。
- [~] 扩展接口基础：已有 `ModelProvider`、`Tool`、Registry 和权限接口；Skills／Hooks 的完整契约与实现按 S4 验收。

### S0 必须先核对并修复的基线问题

以下来自对 `28ac4dd` 的静态代码审查，尚未运行复现；实施时先增加能证明问题的回归用例。

- [ ] **shell 实时转发**：`crates/forge-tools/src/shell.rs` 创建了 `forward` future，但没有 await／并发轮询；将管道读取、转发与进程等待真正并发驱动。
- [ ] **输出内存有界**：`read_pipe` 超过 `MAX_CAPTURE_BYTES` 后仍向 `buf` 追加；改为有界捕获，继续排空管道以免进程堵塞；事件队列同样要有背压或合并策略。
- [ ] **结束状态完整**：请求失败、流中断、工具失败、压缩失败、通道关闭、取消都必须有明确终态；异常后恢复可输入状态，并在退出／panic 时恢复终端。
- [ ] **压缩消息完整性**：保留系统规则；裁剪时按工具调用与结果成组处理；多工具尚未全部完成时，不生成丢失调用方的孤立结果。为这些情况补协议级回归用例。

## 1. 工具体系

- [ ] **Read／Edit／Write（S2）**：按行／范围读取；精确替换和唯一性校验；修改前检查文件是否已变化；diff 预览、原子写入、失败不落盘；保留编码／换行，覆盖中文路径、CRLF 和 BOM。
- [ ] **Grep／Glob（S2）**：优先复用 ripgrep；结构化结果、范围限制、分页／截断提示；遵守忽略规则，显式包含被忽略文件时有清晰反馈。
- [ ] **Todo（S2）**：模型可维护任务清单，界面显示状态；任务与会话一同恢复，取消／失败不会误报完成。
- [ ] **Task（S5）**：派生独立子任务，可选择模型和工具权限；生命周期、隔离与汇总统一由第 2 节管理。
- [ ] **WebSearch／WebFetch（S4）**：可配置搜索来源；返回标题、URL、时间及内容；超时／大小限制；遵守网络策略，区分外部内容与用户指令。
- [ ] **并行工具（S4）**：先支持相互独立的只读调用；依赖操作、同文件写入和共享状态修改串行执行；结果按 call ID 对应，权限与预算统一核算。
- [ ] **后台任务（S4）**：dev server／watch 可转后台；可列出、读取增量输出、停止，记录工作目录与所属任务；输出有界、退出清理、进程树回收、恢复时核对进程身份。
- [ ] **PTY／交互输入（S7）**：有实际交互命令需求时加入伪终端、stdin、尺寸变化和终端控制序列处理；不作为普通后台命令的前置条件。

## 2. 多 agent 协作与跨终端交流（S5）

- [ ] **子代理运行**：spawn／join／查询／取消，独立上下文，可指定不同模型；并发数、嵌套深度和总任务预算受限，父任务取消向下传播。
- [ ] **权限与结果**：子代理权限不超过父任务授权；回传结论、修改文件、测试证据和未决问题；主代理审阅后集成。
- [ ] **工作区隔离**：写任务使用独立 worktree；共享仓库元数据操作协调执行；明确冲突处理、失败任务保留与清理策略。
- [ ] **跨终端交流**：本地命名管道／socket；消息含项目、会话、任务和关联请求标识；明确请求／回复／事件格式、本机访问控制、超时与断线行为。
- [ ] **协议公开化**：版本号、能力协商、错误码和示例；子代理与跨终端尽量复用运行协议，避免维护两套任务语义。

## 3. 权限与安全

- [ ] **统一权限层（S2）**：allow／ask／deny；按工具、路径及命令规则判断；TUI 确认本次／本会话／持久规则；无界面时按明确策略处理 ask，不能默认为放行。
- [ ] **命令与路径边界（S2）**：处理引号、管道、重定向、命令替换等 shell 结构；无法可靠识别时要求审批；路径规范化并检查链接／junction 越界。命令前缀匹配不能代替沙箱。
- [ ] **Windows 沙箱（S2 基础／S4 扩展）**：工作区写入与网络边界；read-only／auto／full-access 的语义清晰；检测平台能力，隔离不可用时明确显示并选择受限模式，不静默降级。
- [ ] **工作区与扩展信任（S2 基础／S4 扩展）**：区分项目规则与可执行配置；不因打开仓库自动执行其中的 hooks／MCP 命令；扩展仍经过统一权限、网络和预算控制。
- [ ] **密钥与脱敏（S3）**：系统凭据存储，保留环境变量方式及旧配置迁移路径；日志、诊断包、导出记录不输出凭据。

## 4. 上下文工程

- [ ] **原始记录与上下文分离（S1）**：原始对话／工具事件追加保存，压缩只生成模型上下文视图；保存摘要来源和版本，支持查看、导出与定位原文。
- [ ] **约束与工具配对（S0 基础／S3 完善）**：系统规则和显式约束有稳定来源；压缩不破坏待完成任务、工具调用／结果配对；为输出和工具定义预留窗口容量。
- [ ] **文件引用（S3）**：`@文件`／`@目录`／行范围，按需读取；区分 `.gitignore`、forge 上下文排除规则和权限限制；显示来源与文件是否已变更。
- [ ] **上下文可视化（S3）**：查看当前规则、skills、引用文件、摘要及估算占用；移除过期引用、固定关键材料；明确本地估算与 API 实际 usage 的差异。
- [ ] **缓存优化与命中率（S3）**：稳定提示前缀与工具顺序；按 Provider 能力使用自动缓存／`cache_control`；按实际返回字段展示命中量，不支持时标为未知。
- [ ] **窗口与思考限额（S3）**：上下文进度条、压缩预警；reasoning effort／thinking budget 可配置、可按会话调整；校验模型能力。
- [ ] **模型主动提问（S2）**：AskUserQuestion 式选择题与自由输入；审批、澄清和普通消息分别处理，支持取消及无头模式的明确失败／预设回答策略。

## 5. 模型接入与 Provider

- [ ] **模型能力表（S3）**：完善 Kimi／DeepSeek／GLM／Qwen 等实际需要的方言；配置文本／图片、工具调用、上下文容量、输出上限和思考参数；能力缺失时清晰报错或经用户选择降级。
- [ ] **模型发现与切换（S3）**：`/v1/models` 自动获取、`/models` 切换，保留手工配置；模型列表不等于能力表，不支持发现的端点仍可使用。
- [ ] **分类重试与回退（S3）**：仅对可恢复错误退避重试，遵守 Retry-After，次数可配置（初始默认至多 5 次）；鉴权／参数错误直接说明；上下文超限走裁剪／压缩；流式中断保留已收到内容并标为未完成。
- [ ] **回退兼容与执行边界（S3）**：切换前检查模型能力和窗口，转换或拒绝不兼容历史；重试模型请求不代表可以重放已执行工具，有副作用操作遵守第 12 节恢复规则。
- [ ] **用量与价格（S1 计账基础／S3 展示）**：按请求累计到轮次、任务和日期；涵盖重试、压缩和辅助模型调用，后续纳入子代理；按缓存／输入／输出等字段计账，价格可配置并标注估算，未知价格不记作零成本。
- [ ] **OAuth 登录（S7）**：按厂商实际提供的接入能力评估；支持登录／刷新／注销与凭据存储；不能预设网页订阅自动等同于通用 API 授权。
- [ ] **订阅额度（S7）**：在可取得可信额度数据的接入上展示窗口和剩余额度；周期按实际返回或配置定义，不写死各家都是 5 小时／周额度；缺数据时明确未知。

## 6. TUI／交互体验

- [ ] **运行中控制（S1）**：停止当前任务和退出应用分开；显示取消进度；用户可追加指令，明确“在下一个安全边界纠正”或“排队下一轮”，不声称撤销已经发生的副作用。
- [ ] **输入编辑（S3）**：多行输入、光标移动、输入历史、快捷键、外部编辑器；大段粘贴识别、中文宽度与 Unicode 组合字符处理。
- [ ] **导航与渲染（S3）**：滚动／复制／搜索输出、工具记录折叠、长会话按可见区域渲染；Markdown 标题、列表、代码、表格和 diff 高亮；落库保留原始 Markdown。
- [ ] **斜杠命令与文件补全（S3）**：`/` 命令菜单、模糊过滤、Tab／Enter 选择；与 `@文件` 引用共用清晰的输入交互。
- [ ] **运行反馈（S3）**：token、tok/s、每轮／工具耗时；区分等待响应、生成／推理、执行、等待审批、取消、失败；流式统计口径明确。
- [ ] **命令用途说明（S2）**：展示模型提供的简短用途、实际命令／工作目录及权限理由；审批的实际参数与展示内容一致。
- [ ] **标题与会话入口（S3）**：自动短标题和手动重命名；标题生成失败不影响主任务，额外费用纳入用量。
- [ ] **图片输入（S3）**：剪贴板／文件图片，预览、移除、大小限制；通过统一附件模型传给具备图片能力的 Provider；持久化后恢复仍可读取附件。
- [ ] **回退交互（S2 基础／S3 完善）**：明确选择回退会话、代码或两者；先预览影响，再执行第 7 节的检查点恢复。

## 7. Git 集成

- [ ] **检查点与回退（S2）**：记录任务前基线及 agent 改动；保留用户原有未提交／未跟踪文件、暂存区状态及后来手动改动；冲突时停止并展示，不直接用 reset／clean／stash 覆盖工作区。非 Git 目录明确禁用代码回退或使用文件快照。
- [ ] **worktree 隔离（S5）**：任务创建、分支关联、结果集成与清理；检查未提交改动再删除 worktree；复用第 2 节协作规则。
- [ ] **Git 使用规范（S2）**：规范提交信息、分批提交、按用户授权执行 push；权限层约束有副作用命令，不能仅依赖系统提示词。
- [ ] **commit／PR 辅助（S3）**：展示和审查未提交变更、生成提交信息与 PR 描述；明确修改来源和测试证据。

## 8. 项目记忆与系统提示词

- [ ] **AGENTS.md（S2）**：全局／项目根／相关子目录按作用域加载，明确覆盖与冲突规则；进入其他目录时刷新相关指令，展示实际来源，不把所有子目录规则无差别塞进上下文。
- [ ] **`/init`（S3）**：扫描后生成项目结构、构建／测试命令与约定；已有文件先展示差异，保留用户自定义内容。
- [ ] **系统提示词（S2 基础／持续评测）**：工具策略、局部修改、多步计划、错误恢复、输出风格与权限边界；与真实工具能力一致，调整前后运行固定任务评测。
- [ ] **计划模式（S2）**：只读探索、产出计划、获批后执行；只读限制由权限层落实，覆盖 shell 和后续扩展；计划可编辑、保存及恢复。

## 9. MCP 与 Skills（S4）

- [ ] **MCP client**：评估 `rmcp` 接入统一 Registry；配置／连接／重连／关闭管理，工具命名冲突、参数 schema、超时、取消与错误归一化。
- [ ] **MCP 兼容范围**：明确采用的协议版本、stdio／HTTP 等传输、认证方式及 tools／resources／prompts 支持范围；逐项标注支持、待做或暂不支持，配代表性服务契约测试。
- [ ] **Skills 加载**：支持 `SKILL.md`、元数据及相对资源路径；先加载摘要，命中后读正文／资源；触发规则和 `/skill` 显式调用可配置，冲突可诊断。
- [ ] **生态兼容清单**：说明目录约定、字段、命令语义及兼容对象／版本；用真实样例测试，不以相同名称声称与其他 agent 完全兼容。
- [ ] **内置扩展**：提供少量已验收 skills 和推荐 MCP 配置，按需启用；版本可追踪，启动失败可定位，卸载不残留进程。

## 10. Hooks（S4）

- [ ] **生命周期契约**：before／after agent、model、tool，以及 error／cancel／session 等事件；定义输入输出 schema、执行顺序、超时、退出码和阻断语义。
- [ ] **改写与权限**：明确哪些字段允许修改；改写后重新校验参数与权限；限制递归触发，hook 不能绕过运行策略或使失败误报成功。
- [ ] **管理与诊断**：用户／项目配置作用域、显式信任、禁用／调试入口、日志脱敏及代表性脚本兼容测试。

## 11. 旁路对话与便捷命令

- [ ] **`/btw`（S3）**：不写入主会话的临时侧问；明确携带哪些上下文，结束后不自动合并；仍有自身上下文消耗，计入总用量与预算。
- [ ] **帮助与命令说明（S3）**：`/help` 反映当前可用能力、快捷键及禁用原因；Markdown 渲染与落库保真统一归第 6 节，跨终端协议统一归第 2 节。

## 12. 运行控制与故障恢复（S1）

- [ ] **状态与标识**：统一 session／turn／task／tool-call ID；运行、等待审批、取消中、成功、失败、已取消各有明确语义；同一会话并发提交受控，每次运行有唯一终态。
- [ ] **取消与资源回收**：取消传到 Provider、工具和子进程树；Windows 检查 Job Object 等进程树管理方案；设置退出清理期限、必要时强制终止，记录未完成操作。
- [ ] **任务预算**：步骤数、总时长、token／估算费用上限；子任务共享父预算；达到上限停止并解释，用户可明确续跑；不是只限制单次 shell 超时。
- [ ] **循环检测**：识别重复参数调用、相同错误和无进展重试；可配置阈值和恢复方式，避免正常轮询被误杀。
- [ ] **工具执行日志**：持久化调用意图、开始、结果和失败／未知状态；记录结果先于调度后续动作。文件系统或外部副作用与数据库不能天然形成原子事务，崩溃后先核对真实状态。
- [ ] **恢复规则**：已完成操作复用记录；执行状态不明时，先核对文件／进程／外部结果，无法确定则请求用户处理；不能保证 exactly-once，不自动重放未知的写入、提交或外部操作。
- [ ] **错误收口**：请求／流中断／存储／扩展错误分类；保存部分输出并标为未完成；落盘失败必须可见；日志与用户提示可关联定位问题。

## 13. 会话、持久化与数据迁移

- [ ] **追加记录与上下文视图（S1）**：原始记录保真，避免每次新消息全量覆盖会话；压缩、分叉和回退通过独立视图／版本关联，正常操作不抹掉原始证据。
- [ ] **Schema 版本与迁移（S1）**：数据库、消息和附件格式显式版本化；升级前备份，迁移失败保留旧数据；未知版本不能按默认值静默读取。
- [ ] **项目关联（S1 基础／S3 管理）**：保存项目根、工作目录、仓库／分支等必要信息；防止多个进程同时无协调地写入同一会话；恢复到其他目录时明确确认映射。
- [ ] **会话管理（S3）**：按项目列出、搜索、重命名、删除、导出／导入、分叉，指定会话恢复；分叉、会话回退与代码回退分别定义。
- [ ] **数据维护（S3）**：备份／恢复、保留期限、附件清理、损坏检测和可恢复导出；删除会话时清理关联数据，避免孤立附件长期堆积。

## 14. 多端架构与自动化接口

- [ ] **运行接口（S1）**：UI 调用 submit／cancel／approve／reply 等命令并订阅事件；业务编排从 TUI 入口抽出，复用现有 core／trait，只增加当前阶段需要的接口。
- [ ] **无头模式（S3）**：扩展现有 `forge check`，支持 stdin、指定项目／会话／模型、JSON／JSONL 事件和稳定退出码；stdout 数据与 stderr 日志分离；无人值守遇到审批或提问时行为明确，不默默挂起。
- [ ] **传输协议（S6，字段在 S1 预留）**：序号、协议版本、能力协商、错误码、请求去重和审批回执；重连按游标补发，明确事件保留期限和游标过期恢复方式。
- [ ] **Web UI（S6）**：Axum 服务 + React；会话、任务、diff、审批及附件操作；本地访问认证、来源校验，不把本地命令执行服务默认暴露到公网。
- [ ] **桌面端（S6）**：Tauri 2 封装同一运行服务／接口；处理应用启动、窗口关闭、后台进程生命周期与安装卸载；不另写一套 agent 循环。

## 15. 测试、评测与工程质量（从 S0 开始持续交付）

- [ ] **故障回归**：实时输出、大输出、取消、断流、异常退出、存储失败、权限拒绝、压缩配对、并发修改、跨项目恢复等；优先用 mock／故障注入构造可复现用例。
- [ ] **协议契约测试**：三协议的流式分片、工具参数重组、结束原因、usage、错误帧和能力降级；增加 Provider／MCP／Hook 时同时补兼容样例。
- [ ] **固定编程任务集（S2 起）**：覆盖定位 bug、小范围修改、跨文件修改、测试失败恢复和长上下文续作；记录成功率、成本、耗时与人工干预，保存模型／配置／提示词版本。
- [ ] **CI 与边界**：在现有 Windows `cargo test`／build 上补 fmt、clippy 及必要契约检查；公共接口和关键约定有简短说明，核心不依赖 UI；测试验证行为，不靠堆数量判断质量。
- [ ] **性能预算（S0 基础／S3 基准）**：为启动、首个可见事件、长会话内存、滚动和大输出设定可测目标；先记录基线，再定义回归门槛。
- [ ] **诊断与可观测性**：结构化脱敏日志，关联会话／请求／工具标识、耗时及错误；诊断信息可导出，API key 等敏感字段默认排除。

## 16. 配置、安装与发布

- [ ] **配置分层（S1 格式基础／S3 完善）**：明确全局 → 项目 → 会话 → 显式命令行覆盖顺序；环境变量用于约定字段；校验类型与冲突，显示最终生效来源，提供旧配置迁移。
- [ ] **首次启动与 doctor（S3）**：引导选择 Provider／模型和密钥方式；检查 Git Bash、工作目录、数据库、网络及 Provider，后续增加 MCP／扩展诊断。
- [ ] **基础发行（S3）**：Windows 预编译包、校验信息、版本号、安装／卸载说明和更新日志；安装包 smoke test，升级前备份配置与数据。
- [ ] **更新与平台扩展（S7）**：按需求加入更新检查／自动更新、包管理器与其他平台；评估 shell、凭据、剪贴板、PTY、沙箱差异，各平台独立验收。

## 17. 代码理解（S7 按需）

- [ ] **LSP 能力**：定义／引用／符号查询与诊断；语言服务器启动、重启、工作区管理和资源上限；未安装服务器时仍能使用基础文本搜索。
- [ ] **项目结构与增量索引**：生成模块／符号概览，文件变化后失效或更新；先证明对固定任务集有帮助，再考虑更复杂索引方案。

---

## 完成标准

工具广度、权限、项目规则和扩展生态是功能面；**可取消、可恢复、数据完整、成本受控、协议兼容及持续回归**决定能否稳定使用。各阶段以表中的可验证行为为准，不以“补齐若干功能就等同于生产级 agent”作为结论。
